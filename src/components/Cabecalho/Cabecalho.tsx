import Menu from "../Menu/Menu";

export default function Cabecalho() {
  return (
    <header className="fixed top-0 flex justify-center items-center w-full h-20 p-3 bg-slate-800">
        <Menu/>
    </header>
  )
}
